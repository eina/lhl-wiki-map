<!-- site meta + body tag -->
<%- include('partials/_site-head') %>

<!-- <header> + <nav> -->
<%- include('partials/_headnav') %>

<main>
  <h1>Create A Map</h1>
  <form id="createMapForm">
    <label class="form-group">
      <span class="form-label">This map is a collection of</span>
      <input
        type="text"
        name="map-title"
        id="map-title"
        class="form-input input-lg"
        placeholder="eg. Best Coffee in Town"
      />
    </label>

    <div class="btn-container">
      <button type="submit" class="btn btn-primary">Save</button>
      <!-- hide if path is not maps/edit -->
      <button type="button" class="btn btn-error">Delete</button>
    </div>
  </form>

  <div id="leaflet-map" class="map full-width"></div>

  <h3>My Places</h3>
  <!-- id needs to be unique identifier -->
  <div class="card-grid place-grid">
    <!-- id has to be place and id -->
    <div class="card s-rounded" id="place-1">
      <div class="card-image">
        <img class="img-responsive" src="https://picsum.photos/300/150" />
      </div>
      <div class="card-header">
        <div class="card-title h5">[Place Title]</div>
      </div>
      <div class="card-body">
        <p>[Place Description] Some description about this awesome place I put on the map yay.</p>
      </div>
      <div class="card-footer text-center">
        <button class="btn edit-place" type="button">Edit</button>
        <button class="btn" type="button">Delete</button>
      </div>
    </div>

    <!-- <div class="card s-rounded" id="edit-place2">
      <form id="addPlace">
        <div class="card-body">
          <label class="form-group">
            <span class="form-label">Name</span>
            <input type="text" class="form-input" id="edit-place2-name" name="place-name" />
          </label>
          <label class="form-group">
            <span class="form-label">Image URL</span>
            <input type="text" class="form-input" id="edit-place2-img" name="place-img" />
          </label>
          <label class="form-group">
            <span class="form-label">Description</span>
            <textarea class="form-input" id="edit-place2-desc" rows="3"></textarea>
          </label>
        </div>
        <div class="card-footer">
          <button class="btn edit-place" type="button">Edit</button>
          <button class="btn" type="button">Delete</button>
        </div>
      </form>
    </div> -->
  </div>
</main>
<%- include('partials/_site-foot') %>
